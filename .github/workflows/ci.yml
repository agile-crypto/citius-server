name: CI

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  ci:
    name: ci          # <-- this is the status-check name you'll reference in branch protection
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Install buf
        uses: bufbuild/buf-setup-action@v1

      - name: Install golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          install-mode: binary
          skip-cache: false

      - name: Run CI gate
        run: make ci
